import {
    Box,
} from "@mui/material";


export type SettingsProps = {
}


export const Settings = ({}: SettingsProps) => {

    return <div>
        <Box p={ 2 }>

        </Box>
    </div>
}
